# Calltranslate

A minimal Python HTTP server that returns `hello`.

```bash
python3 app.py
```

The server listens on `0.0.0.0:8000` by default. Override it with the
`HOST` and `PORT` environment variables.
